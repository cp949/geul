# Geul 편집기 문맥

Geul의 저장·변환·편집 계약과 제품 완료 상태를 같은 의미로 해석하기 위한 프로젝트 공통 언어다.

## 문서 모델

**독자 문서**:
Geul이 소유하는 버전형 블록 문서 모델이다. 특정 편집 엔진이나 외부 교환 포맷의 모델과 호환될 필요가 없다.
_Avoid_: Tiptap 문서, ProseMirror 문서, BlockNote 문서

**저장 원본**:
저장과 복원의 권위가 되는 독자 문서 JSON이다. HTML, GFM Markdown과 편집 엔진 상태는 저장 원본에서 만들거나 저장 원본으로 변환되는 표현이다.
_Avoid_: Tiptap JSON, HTML 원본, 에디터 상태

**DOM 투영**:
저장 원본의 구조를 편집기 DOM 속성으로 실어 나르는 표현이다. 일부 항목(열 목록)의 권위를 갖지만 저장 원본 자체는 아니다.
_Avoid_: 저장 원본, 렌더링 결과, 캐시

**안정 ID**:
문서가 저장·복원되거나 지원 포맷을 왕복해도 같은 논리 대상을 식별하는 ID다. 배열 위치나 편집 엔진 위치값은 안정 ID가 아니다.
_Avoid_: 인덱스, ProseMirror position

**논리 셀**:
표의 행과 열이 만드는 하나의 좌표다. 병합된 영역에서는 여러 논리 셀이 하나의 기준 셀에 속할 수 있다.
_Avoid_: 저장된 셀 객체

**기준 셀**:
표에서 실제로 저장되는 셀이며, 자신이 시작하는 논리 좌표와 행·열 span을 소유한다. 병합으로 덮인 논리 좌표에는 별도 기준 셀을 저장하지 않는다.
_Avoid_: 병합 셀의 모든 좌표, 가상 셀

## 변환 계약

**정규형 변환 (canonicalization)**:
의미가 같은 허용 입력을 프로젝트가 정한 하나의 저장 표현으로 바꾸는 과정이다. 허용 여부를 판정하는 계약 검증과 구분한다.
_Avoid_: validation, sanitize

**계약 검증 (validation)**:
값이 독자 문서의 shape와 의미 불변식을 만족하는지 판정하고, 실패 위치와 이유를 구조화해 반환하는 과정이다. 입력을 다른 값으로 고치는 과정이 아니다.
_Avoid_: canonicalization, 자동 보정

**strict export**:
대상 포맷에서 의미 손실이 하나라도 예상되면 결과를 만들지 않고 구조화된 손실 정보를 반환하는 내보내기다.
_Avoid_: 무조건 실패하는 export, 완전 호환 export

**lossy export**:
표현 가능한 결과와 함께 발생한 모든 알려진 의미 손실의 종류와 위치를 반환하는 내보내기다. 보고되지 않은 조용한 손실을 허용한다는 뜻이 아니다.
_Avoid_: best-effort export, 경고 없는 변환

## 제품 상태

**기능 기준선**:
제품이 제공하기로 검토한 기능 집합과 각 기능의 목표 수준을 고정한 버전별 기준이다. 참고 제품의 이후 변경은 자동으로 포함되지 않는다.
_Avoid_: 최신 BlockNote 기능, 구현 목록

**완료 판정**:
승인된 체크리스트의 최신 증거와 열린 결함을 기준으로 단계 완료 여부를 결정하는 행위다. 구현 파일의 존재나 일부 테스트 통과만으로 완료를 뜻하지 않는다.
_Avoid_: 테스트 통과, 코드 완료

## UI 오버레이

**해제형 오버레이**:
바깥 클릭·Escape로 닫히고, 트리거(앵커)가 열리는 동안 항상 뷰포트 안에 있다고 가정하는 오버레이다. 메뉴·툴바·팝오버가 해당하며, 뷰포트를 넘는 부분은 clamp로 안쪽에 접어 넣는다. 바깥 클릭으로 닫힐 때도 그 클릭 대상 자신의 동작은 예외 없이 함께 실행된다 — dismiss가 클릭 대상의 동작을 가로막지 않는다(Issue #155).
_Avoid_: 그냥 "오버레이", 메뉴, 팝오버

**앵커 오버레이**:
특정 DOM 요소(행·열·미디어 경계 등)에 시각적으로 고정되고, 바깥 클릭·Escape로 닫히지 않는 hover·selection 기반 오버레이다. 그 요소가 뷰포트 밖으로 나가도 clamp하지 않는다 — 대상에서 분리하면 어떤 요소를 가리키는지 알 수 없어지므로, 접근성은 뷰포트 clamp가 아니라 네이티브 스크롤 도달성으로 확보한다.
_Avoid_: 해제형 오버레이, 팝오버

**자동 닫힘**:
앵커 오버레이가 가리키던 대상의 안정 ID가 더 이상 존재하지 않게 되면(삭제·undo 등) 오버레이를 즉시 닫는 동작이다. 인덱스가 범위 안에 있어도 안정 ID가 사라졌으면 무효화로 판정한다.
_Avoid_: 재조준, 무효화 감시

**재조준**:
앵커 오버레이가 가리키던 대상의 안정 ID는 살아있지만 앞선 행/열이 사라져 인덱스만 밀린 경우, 오버레이를 닫지 않고 새 인덱스로 갱신하는 동작이다. 오버레이 내부 상태(예: 열려 있던 실패 알림)는 재조준으로 유지된다 — 자동 닫힘과 달리 상태가 보존된다는 점이 둘을 구분하는 경계다.
_Avoid_: 자동 닫힘, 재초기화

**활성 바**:
표 행/열 그립(앵커 오버레이)의 중간 노출 상태다. 그 행/열에 텍스트 커서가 있거나 마우스가 그 행/열 위 어디든(꼭 grip 버튼 자리가 아니어도) hover 중이면 뜬다. 커서와 hover가 서로 다른 행/열을 가리키면 최대 2개까지 동시에 뜰 수 있다. 얇은 line으로만 보이며 그 자체로는 클릭·드래그 대상이 아니다.
_Avoid_: idle line, hover 표시, 그립

**grip 버튼**:
활성 바 위에 마우스가 다시 hover(또는 키보드로 focus)했을 때만 pill 크기로 펼쳐지는 실제 버튼이다. 행/열 재정렬 드래그와 클릭(메뉴 열기)은 이 단계에서만 동작한다 — 활성 바 상태에서는 동작하지 않는다.
_Avoid_: 활성 바, 드래그 핸들, 핸들 아이콘

## 미디어

**업로드 대기**:
파일·이미지·동영상·오디오 블록이 아직 실제 위치를 갖지 못한 상태다. 업로드가 끝나 위치가 정해지기 전까지 저장 원본에 들어갈 수 있는 유효한 중간 상태다.
_Avoid_: placeholder, 빈 미디어 블록

**로컬 프리뷰**:
업로드 콜백이 없을 때 삽입된 미디어를 편집 세션 동안 화면에 보여주는 임시 표시 상태다. 저장 원본을 왕복하지 않으며, 저장 후 다시 열면 업로드 대기로 되돌아간다.
_Avoid_: Blob URL, 임시 이미지, 캐시 이미지
