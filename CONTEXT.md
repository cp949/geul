# Geul 편집기 문맥

Geul의 저장·변환·편집 계약과 제품 완료 상태를 같은 의미로 해석하기 위한 프로젝트 공통 언어다.

## 문서 모델

**독자 문서**:
Geul이 소유하는 버전형 블록 문서 모델이다. 특정 편집 엔진이나 외부 교환 포맷의 모델과 호환될 필요가 없다.
_Avoid_: Tiptap 문서, ProseMirror 문서, BlockNote 문서

**저장 원본**:
저장과 복원의 권위가 되는 독자 문서 JSON이다. HTML, GFM Markdown과 편집 엔진 상태는 저장 원본에서 만들거나 저장 원본으로 변환되는 표현이다.
_Avoid_: Tiptap JSON, HTML 원본, 에디터 상태

**DOM 투영**:
저장 원본의 구조를 편집기 DOM 속성으로 실어 나르는 표현이다. 일부 항목(열 목록)의 권위를 갖지만 저장 원본 자체는 아니다.
_Avoid_: 저장 원본, 렌더링 결과, 캐시

**안정 ID**:
문서가 저장·복원되거나 지원 포맷을 왕복해도 같은 논리 대상을 식별하는 ID다. 배열 위치나 편집 엔진 위치값은 안정 ID가 아니다.
_Avoid_: 인덱스, ProseMirror position

**논리 셀**:
표의 행과 열이 만드는 하나의 좌표다. 병합된 영역에서는 여러 논리 셀이 하나의 기준 셀에 속할 수 있다.
_Avoid_: 저장된 셀 객체

**기준 셀**:
표에서 실제로 저장되는 셀이며, 자신이 시작하는 논리 좌표와 행·열 span을 소유한다. 병합으로 덮인 논리 좌표에는 별도 기준 셀을 저장하지 않는다.
_Avoid_: 병합 셀의 모든 좌표, 가상 셀

## 변환 계약

**정규형 변환 (canonicalization)**:
의미가 같은 허용 입력을 프로젝트가 정한 하나의 저장 표현으로 바꾸는 과정이다. 허용 여부를 판정하는 계약 검증과 구분한다.
_Avoid_: validation, sanitize

**계약 검증 (validation)**:
값이 독자 문서의 shape와 의미 불변식을 만족하는지 판정하고, 실패 위치와 이유를 구조화해 반환하는 과정이다. 입력을 다른 값으로 고치는 과정이 아니다.
_Avoid_: canonicalization, 자동 보정

**strict export**:
대상 포맷에서 의미 손실이 하나라도 예상되면 결과를 만들지 않고 구조화된 손실 정보를 반환하는 내보내기다.
_Avoid_: 무조건 실패하는 export, 완전 호환 export

**lossy export**:
표현 가능한 결과와 함께 발생한 모든 알려진 의미 손실의 종류와 위치를 반환하는 내보내기다. 보고되지 않은 조용한 손실을 허용한다는 뜻이 아니다.
_Avoid_: best-effort export, 경고 없는 변환

## 제품 상태

**기능 기준선**:
제품이 제공하기로 검토한 기능 집합과 각 기능의 목표 수준을 고정한 버전별 기준이다. 참고 제품의 이후 변경은 자동으로 포함되지 않는다.
_Avoid_: 최신 BlockNote 기능, 구현 목록

**완료 판정**:
승인된 체크리스트의 최신 증거와 열린 결함을 기준으로 단계 완료 여부를 결정하는 행위다. 구현 파일의 존재나 일부 테스트 통과만으로 완료를 뜻하지 않는다.
_Avoid_: 테스트 통과, 코드 완료
