diff --git a/dev/lib/edit-map.js b/dev/lib/edit-map.js
index 983d0556e2713dc92982faa6a09b1093ef4d02db..7fa565f487c48d65cb2d30729c2aa8b7281cb88d 100644
--- a/dev/lib/edit-map.js
+++ b/dev/lib/edit-map.js
@@ -34,6 +34,14 @@ export class EditMap {
      * @type {Array<Change>}
      */
     this.map = []
+
+    /**
+     * `at` -> index in `this.map`. O(1) lookup for `addImplementation`
+     * instead of a linear scan (cp949/geul#26).
+     *
+     * @type {Map<number, number>}
+     */
+    this.indexByAt = new Map()
   }
 
   /**
@@ -124,6 +132,7 @@ export class EditMap {
 
     // Truncate everything.
     this.map.length = 0
+    this.indexByAt.clear()
   }
 }
 
@@ -137,31 +146,29 @@ export class EditMap {
  * @returns {undefined}
  */
 function addImplementation(editMap, at, remove, add) {
-  let index = 0
-
   /* c8 ignore next 3 -- `resolve` is never called without tables, so without edits. */
   if (remove === 0 && add.length === 0) {
     return
   }
 
-  while (index < editMap.map.length) {
-    if (editMap.map[index][0] === at) {
-      editMap.map[index][1] += remove
+  const existingIndex = editMap.indexByAt.get(at)
 
-      // To do: before not used by tables, use when moving to micromark.
-      // if (before) {
-      //   add.push(...editMap.map[index][2])
-      //   editMap.map[index][2] = add
-      // } else {
-      editMap.map[index][2].push(...add)
-      // }
+  if (existingIndex !== undefined) {
+    const entry = editMap.map[existingIndex]
+    entry[1] += remove
 
-      return
-    }
+    // To do: before not used by tables, use when moving to micromark.
+    // if (before) {
+    //   add.push(...entry[2])
+    //   entry[2] = add
+    // } else {
+    entry[2].push(...add)
+    // }
 
-    index += 1
+    return
   }
 
+  editMap.indexByAt.set(at, editMap.map.length)
   editMap.map.push([at, remove, add])
 }
 
diff --git a/lib/edit-map.js b/lib/edit-map.js
index ecc8bce784d29a48e0869be1c4a2dd5ccf1d3d01..d574e8cf2a3da5c2a51ca77889943578c223679f 100644
--- a/lib/edit-map.js
+++ b/lib/edit-map.js
@@ -34,6 +34,14 @@ export class EditMap {
      * @type {Array<Change>}
      */
     this.map = [];
+
+    /**
+     * `at` -> index in `this.map`. O(1) lookup for `addImplementation`
+     * instead of a linear scan (cp949/geul#26).
+     *
+     * @type {Map<number, number>}
+     */
+    this.indexByAt = new Map();
   }
 
   /**
@@ -117,6 +125,7 @@ export class EditMap {
 
     // Truncate everything.
     this.map.length = 0;
+    this.indexByAt.clear();
   }
 }
 
@@ -130,28 +139,29 @@ export class EditMap {
  * @returns {undefined}
  */
 function addImplementation(editMap, at, remove, add) {
-  let index = 0;
-
   /* c8 ignore next 3 -- `resolve` is never called without tables, so without edits. */
   if (remove === 0 && add.length === 0) {
     return;
   }
-  while (index < editMap.map.length) {
-    if (editMap.map[index][0] === at) {
-      editMap.map[index][1] += remove;
-
-      // To do: before not used by tables, use when moving to micromark.
-      // if (before) {
-      //   add.push(...editMap.map[index][2])
-      //   editMap.map[index][2] = add
-      // } else {
-      editMap.map[index][2].push(...add);
-      // }
 
-      return;
-    }
-    index += 1;
+  const existingIndex = editMap.indexByAt.get(at);
+
+  if (existingIndex !== undefined) {
+    const entry = editMap.map[existingIndex];
+    entry[1] += remove;
+
+    // To do: before not used by tables, use when moving to micromark.
+    // if (before) {
+    //   add.push(...entry[2])
+    //   entry[2] = add
+    // } else {
+    entry[2].push(...add);
+    // }
+
+    return;
   }
+
+  editMap.indexByAt.set(at, editMap.map.length);
   editMap.map.push([at, remove, add]);
 }
 
