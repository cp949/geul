# 20260825-01 react 허용 표면 등 아키텍처 불변식 단일화(#77)

- 레인: qq-workflow (단계 1~4)
- 대상 이슈: #77(종료)
- 작업 브랜치: `docs/77-react-surface-doc-ownership`(`dev` ff-only 이전 후 삭제)

## 목표

`react` 허용 표면 문장이 `docs/adr/0002-enforce-layered-package-boundaries.md`(ADR-0002)·`AGENTS.md`·`README.md` 세 곳에 원본처럼 복제돼 있던 것을 ADR-0002 단일 원본 + 참조 구조로 정리한다. 계획 단계 스캔에서 동일 3곳 복제 패턴 2건(`model`/`io` DOM·프레임워크 비의존, `core` 공개 타입 Tiptap/PM 비노출)을 추가로 확인해 사용자 승인 후 같은 DELTA에서 함께 처리했다.

## 확정 커밋 해시

| 해시 | 제목 |
| --- | --- |
| `01a79c0` | docs(agents,readme): react 허용 표면 등 아키텍처 불변식 3문장을 ADR-0002 참조로 바꾼다 |

작업 브랜치 커밋 1개는 이미 단일 의미 단위라 재조립하지 않고 그대로 `dev`에 fast-forward 이전했다(`refs/backup/<브랜치>-pre-squash` 대비 트리 diff 빈 출력).

## 바꾼 계약과 파일

공개 계약 변경 없음 — 문서 서술만 바꿨다.

- `AGENTS.md` — "아키텍처 불변식" 절 bullet 3개(`model`/`io` 비의존, `core` 타입 비노출, `react` 허용 표면)를 구체 계약 재서술 없이 ADR-0002를 가리키는 포인터 문장으로 교체.
- `README.md` — `packages/react` 목록 줄 1개, 아키텍처 절 마지막 문장 1개를 같은 방식으로 ADR-0002 링크로 교체.
- `docs/adr/0002-enforce-layered-package-boundaries.md` — 수정 없음. 3개 사실(모델/io 비의존, core 타입 비노출, react 허용 표면·`@tiptap/react` 비의존)을 본문·Consequences에 이미 모두 포함하고 있어 유일한 원본으로 확정.

파일 2개(`+5/-5`).

## 실행한 검증과 결과

단계-3 진입, 단계-4 병합 직전 `pnpm verify` 전량 2회, 모두 통과(biome lint·turbo build 5/5·typecheck 10/10·root vitest·boundary·license·playwright e2e 112/112 — chromium/firefox/webkit).

읽기 전용 subagent 결함 탐지: BLOCKER/MAJOR/MINOR 0건. 참고 관찰 1건(AGENTS.md 세 bullet이 "ADR-0002가 소유한다" 문형을 반복 — 서로 다른 사실을 가리키므로 복제 아님, 스타일 취향으로 미수정).

## 남은 제한

- 문서만 변경했다. 실제 패키지 경계는 `pnpm check:boundaries`와 `packages/core/test/public-types.test.ts`가 이미 강제하며 이번 변경은 그 강제를 바꾸지 않는다.
- 의존 방향 ASCII 다이어그램(각 파일의 ` ```text ` 블록) 복제는 이슈 제외범위라 건드리지 않았다 — AGENTS.md·README.md가 각자 도식을 갖는 것은 별건 판단 대상으로 남아 있다.

## 등록한 이슈와 pitfall

- 신규 이슈 등록 없음.
- 완료 댓글 1건 게시 후 #77 종료.
- pitfall·가이드 신규 승격 없음.

## 절차상 기록

- 리뷰 단계를 생략하지 않았다 — 단계-3(완료 조건 대조 + 결함 탐지)을 실행했고 `IMPL-REVIEW-01`이 남았다.
- 완료 댓글 초안(`pending-issues/01.md`)은 단계-1~3이 아니라 단계-4에서 `IMPL-REVIEW-01.md`의 검증된 내용을 근거로 작성했다 — `qq-workflow.md`가 이 초안 작성을 어느 단계에도 명시 배정하지 않은 절차상 공백으로, `ff-workflow.md`(`20260824-03` 이력)에서 이미 같은 공백이 관측됐다. 이번에도 절차 문서 자체는 고치지 않고 여기에만 기록한다 — 문서 수정 여부는 사용자 판단이 필요하다.
