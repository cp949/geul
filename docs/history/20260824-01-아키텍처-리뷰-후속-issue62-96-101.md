# 20260824-01 아키텍처 리뷰 후속(#62·#96·#101)

- 레인: ff-workflow (트랙 0~8)
- 대상 이슈: #62(종료), #96(종료), #101(종료)
- 작업 브랜치: `chore/62-96-101-arch-review-followups`(`dev` ff-only 이전 후 삭제)

## 목표

2026-08-22 테스트 아키텍처 리뷰가 남긴 후속 이슈 중 마지막 세 건을 처리한다. 기능 변화는 없다.

- **#62** — `table-handles.test.tsx`의 드래그 재정렬 회귀 테스트가 `pointerMove` 좌표 누락으로 vacuous-pass였던 문제. 조사 결과 `dev`에 이미 반영된 수정으로 완료.
- **#96** — `check:boundaries`가 `packages/react`의 Tiptap 의존을 막지 않는다는 의심. 조사 결과 게이트 구멍이 아니었다 — 실제 강제 주체는 `tests/workspace-boundaries.test.ts`(`pnpm test`)이고, 오귀속된 주석만 정정.
- **#101** — 붙여넣기 테스트의 `cellPos + 2` 매직넘버에 근거가 없던 문제. `+2`가 `prosemirror-tables`의 `cellNear` 폴백 우연 의존이었음을 확정하고 공용 헬퍼(`cellId` + `findCellBoundaryPosition`/`placeCaretInCell`, `+1` 규칙)로 교체.

## 확정 커밋 해시

| 해시 | 제목 |
| --- | --- |
| `5a43029` | test(react): table-selection-toolbar.test.tsx의 ADR-0002 인용 주석을 실제 강제 주체·문구 범위에 맞게 정정한다 |
| `c5ec7dc` | test(react): table-handle-menu·table-handles 테스트의 ADR-0002 인용 주석을 실제 강제 주체·문구 범위에 맞게 정정한다 |
| `58c025c` | test(core): 손상된 표 붙여넣기 테스트의 cellPos+2 매직넘버를 cellId 기반 헬퍼로 교체한다 |

작업 브랜치 커밋 5개(트랙-4 구현 3개 + DELTA-01a 리뷰 픽스 1개 + 트랙-6 F1 수정 1개)를 의미 단위 3개로 재그룹화했다 — 같은 파일(`table-selection-toolbar.test.tsx`)을 손댄 DELTA-01 구현 커밋과 트랙-6 F1 수정 커밋을 한 그룹으로, DELTA-01a 구현과 그 리뷰 픽스를 한 그룹으로 합쳤다. 상쇄하는 줄 왕복은 없었다(재대조 2회 모두 `refs/backup/<브랜치>-pre-squash` 대비 빈 출력). 기준선은 `ae5e38f`, `dev`는 fast-forward됐다.

## 바꾼 계약과 파일

공개 계약 변경 없음 — 전부 테스트 파일 내부(주석 정정, 헬퍼 교체)다.

- `packages/react/test/table-selection-toolbar.test.tsx` — ADR-0002 인용 주석 7곳을 "ADR-0002 결정 본문에서 파생하는 `@tiptap/pm` 의존 금지 — Consequences가 이름으로 적은 것은 `@tiptap/react`뿐이다" 계열로 통일. `"check:boundaries가 강제"` 오귀속 문구 제거, 다른 테스트 파일 이름을 인용하지 않고 메커니즘만 서술(`PIT-0022` 회피).
- `packages/react/test/table-handles.test.tsx`, `table-handle-menu.test.tsx` — 같은 ADR-0002 파생 언어로 주석 각 1곳 정정(두 파일이 중복 주석).
- `packages/core/test/editor-controller-table-paste.test.ts` — 인라인 `cellPos + 2` 순회(손상 `setNodeMarkup`, 캐럿 배치, 복원 `setNodeMarkup` 세 자리)를 `cellId` + `findCellBoundaryPosition`/`placeCaretInCell` 공용 헬퍼로 교체. 최초 `cellId` 확보가 `findCellBoundaryPosition`으로 대체 불가능한 이유(순환 의존)를 근거 주석으로 추가.

파일 4개(`+49/-19`).

## 실행한 검증과 결과

트랙-5 진입, 트랙-8 병합 직전까지 `pnpm verify` 전량 2회, 모두 `EXIT_CODE=0`(lint·build·typecheck·unit test·package boundary·license·e2e 전량 통과).

```
pnpm --filter @cp949/geul-react test    Test Files 16 passed(16) / Tests 190 passed(190)
pnpm --filter @cp949/geul-core test     Test Files 22 passed(22) / Tests 381 passed(381)
```

DELTA-02(#101) 완료 조건 4(테스트 제목 집합·개수 불변)는 `dev` 시점 파일을 임시 복원해 `--reporter=json` 전/후 비교로 확인 — before 12건 / after 12건, `fullName` 정렬 목록 완전 일치.

재그룹화 그룹 경계 3곳 전부 focused vitest·typecheck 통과(`table-selection-toolbar.test.tsx` 18/18, `table-handle-menu`+`table-handles`+`table-selection-toolbar` 62/62, `editor-controller-table-paste`+`table-test-support` 22/22).

## 남은 제한

- 트랙-6이 이번 diff가 손댄 4개 파일 밖은 훑지 않았다 — `packages/io`, `packages/model`, `e2e/`에 같은 성격의 ADR-0002 오귀속 인용이나 매직넘버 순회 패턴이 남아 있는지는 이번 범위 밖.
- #96 "게이트 구멍 아님" 판정의 틀렸을 때 비용: `tests/workspace-boundaries.test.ts`가 삭제·완화되면 `packages/react`의 Tiptap 비의존 강제 장치가 실제로 사라지는데 `check:boundaries`만 보는 사람은 못 알아챈다 — 중간 수준. 등록 기준(제품 동작·게이트 구멍·거짓 통과) 미달로 별도 이슈·pitfall로 분리하지 않았다.
- #96 apps/demo·fixtures/consumer 판정의 틀렸을 때 비용: 낮음 — ADR이 현재 두 패키지를 규범 대상으로 선언하지 않아 소급 위반이 아니고, `allowedDependencies`(exact-match) 게이트가 새 `@tiptap/*` 의존을 즉시 잡는다.

## 등록한 이슈와 pitfall

- 완료 댓글 3건 등록 후 #62, #96, #101 종료. 신규 이슈·pitfall 등록 없음 — 이번 실행에서 발견한 결함(트랙-2 MINOR 3건, 트랙-6 MINOR 1건)은 전부 등록 기준(`docs/agents/issue-tracker.md`)을 충족하지 않아 즉시 수정으로 해소했다.

## 절차상 기록

- 리뷰 트랙을 생략하지 않았다 — 트랙-5(누락 탐지)와 트랙-6(결함 탐지)을 모두 실행했고 `IMPL-REVIEW-01`·`IMPL-REVIEW-02`가 남았다.
- 트랙-2가 5라운드 걸렸다(구조적 MAJOR 7건, MINOR 3건, 전부 계획 문서 안에서 해소). 트랙-6이 MAJOR 없이 MINOR 1건(같은 파일 안 정정 절반 누락)을 찾아 즉시 수정했다.
- #62 완료 댓글을 처음 게시할 때 초안 파일의 내부 관리 frontmatter(`종류`·`대상`·`상태` 필드)를 그대로 붙여 넣는 실수가 있었다 — 즉시 `gh issue comment --edit-last`로 정정했다. 이후 두 댓글(#96, #101)은 frontmatter를 제거하고 게시했다.
